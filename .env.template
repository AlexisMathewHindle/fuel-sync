# Supabase Configuration
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# Optional auth redirect override for magic links.
# In local dev you can omit this (defaults to http://localhost:5173).
# In production set this to your deployed app URL, e.g. https://your-site.netlify.app
VITE_MAGIC_LINK_REDIRECT_URL=https://fuel-sync.netlify.app/
